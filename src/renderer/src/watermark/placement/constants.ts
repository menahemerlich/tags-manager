/** גודל מינימלי לשכבת צורה אחרי מיפוי חיתוך, כדי להימנע משאריות קטנות. */
export const WATERMARK_CROP_SHAPE_MIN_PX = 24

/** רוחב מינימלי לסימן מים/שכבה כדי לשמור על שימושיות. */
export const WATERMARK_MIN_LAYER_WIDTH_PX = 40

/** גובה מינימלי לסימן מים/שכבה כדי לשמור על שימושיות. */
export const WATERMARK_MIN_LAYER_HEIGHT_PX = 40

/** רוחב מינימלי למסגרת בחירה (כלי חיתוך/טשטוש). */
export const WATERMARK_SELECTION_MIN_WIDTH_PX = 80

/** גובה מינימלי למסגרת בחירה (כלי חיתוך/טשטוש). */
export const WATERMARK_SELECTION_MIN_HEIGHT_PX = 80

